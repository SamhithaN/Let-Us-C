#include <stdio.h>

int main()
{
    sinx=
    return 0;
}
sin_series()
{
    for(i=2;i<n;i++)
    {
        denom = - denom * x*x;
        num = 2i-1;
        sum = num/denom;
    }
}
rec()
{
    if(num=1)
        return x/1;
    else
       x*x*rec(n-2)/n*rec(n-1)
}
